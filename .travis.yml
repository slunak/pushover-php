language: php
jobs:
    allow_failures:
        - php: 8.2
    include:
        - php: 7.4
          dist: focal
        - php: 8.0
          dist: focal
        - php: 8.1
          dist: focal
        - php: 8.2
          dist: focal
          before_script: sudo apt install libonig5
install: composer install
script: ./vendor/bin/phpunit tests
