language: php
jobs:
    allow_failures:
        - php: 8.2
    include:
        - php: 7.4
          dist: focal
        - php: 8.0
          dist: focal
        - php: 8.1
          dist: focal
        - php: 8.2
          dist: focal
install:
  - sudo apt-get update
  - sudo apt-get install libonig5
  - composer install
script: ./vendor/bin/phpunit tests
